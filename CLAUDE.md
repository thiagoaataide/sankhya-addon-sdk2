# Claude Code

Skill do SDK Sankhya: este repositório (`SKILL.md` + `references/`).

Antes de gerar código de addon, leia `SKILL.md` e valide o `gradle-plugin` ≥ 2.0.18. Referências em `references/` sob demanda.
